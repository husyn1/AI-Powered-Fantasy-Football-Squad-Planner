BACKBOARD_API_KEY=your_backboard_api_key_here

# URL of your deployed frontend (or http://localhost:3000 for local dev)
ALLOWED_ORIGIN=http://localhost:3000

# Optional: lower this in production to reduce LLM costs (default 10)
CHAT_RATE_LIMIT=10

# Set to "production" to disable /docs endpoint
ENV=development
